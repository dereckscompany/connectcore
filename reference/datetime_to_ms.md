# Convert POSIXct to epoch milliseconds

Convert POSIXct to epoch milliseconds

## Usage

``` r
datetime_to_ms(datetime)
```

## Arguments

- datetime:

  (class\<POSIXct\>) the time(s) to convert.

## Value

(numeric) epoch milliseconds (a double).
