% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{abort_stream_error}
\alias{abort_stream_error}
\title{Raise a typed stream error}
\usage{
abort_stream_error(message, url = NULL)
}
\arguments{
\item{message}{(scalar<character>) the condition message (passed through
verbatim).}

\item{url}{(scalar<character> | NULL) the socket URL; query-string credentials
are redacted with \code{\link[=scrub_url]{scrub_url()}} before storing. Default \code{NULL}.}
}
\value{
(class<connectcore_error>) never returns normally; signals the classed
condition described above.
}
\description{
Signals a condition classed \verb{c("connectcore_stream_error",}
\verb{"connectcore_error")} for a \link{StreamClient} transport failure (e.g. calling
\verb{$send()} while the socket is closed). Carries the socket \code{url} (credentials
redacted) when available. The \code{message} is passed through verbatim, so an
existing string stays byte-identical. See \link{connectcore_conditions} for the
taxonomy.
}
\seealso{
\link{connectcore_conditions}
}
