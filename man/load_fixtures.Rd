% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{load_fixtures}
\alias{load_fixtures}
\title{Load JSON fixtures from a directory}
\usage{
load_fixtures(dir, parse = FALSE)
}
\arguments{
\item{dir}{(scalar<character>) a directory holding \verb{*.json} fixture files.}

\item{parse}{(scalar<logical>) if \code{TRUE}, parse each file to a list; if \code{FALSE}
(default), keep the raw JSON string.}
}
\value{
(list) a named list of fixtures (strings or parsed lists), one per
\verb{*.json} file; empty if \code{dir} holds none.
}
\description{
Reads every \verb{*.json} file in \code{dir} and returns a \strong{named} list keyed by each
file's basename without the extension (\code{btc_book.json} -> \code{btc_book}). Each
value is the raw JSON string (\code{parse = FALSE}, the verbatim path that pairs with
\code{\link[=mock_response]{mock_response()}}'s string-body case) or the parsed list
(\code{parse = TRUE}, via \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} with \code{simplifyVector = FALSE}). This
is how a connector loads its real captured fixtures into a route table.
}
