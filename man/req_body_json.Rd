% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{req_body_json}
\alias{req_body_json}
\title{Parse a request's JSON body}
\usage{
req_body_json(req)
}
\arguments{
\item{req}{(class<httr2_request>) the request whose body to parse.}
}
\value{
(list | NULL) the parsed body, or \code{NULL} if the request has no body.
}
\description{
Reads \code{req$body$data} and parses it as JSON, returning a list. It copes with
every shape httr2 stores a body in: a \code{raw} vector or a \code{character} scalar (set
by \code{\link[httr2:req_body]{httr2::req_body_raw()}}, the byte-verbatim path connectors use to sign the
exact body), and an already-deserialised \code{list} (set by \code{\link[httr2:req_body]{httr2::req_body_json()}},
which httr2 serialises only at perform time). Used by body-routed APIs whose
endpoint is encoded in the body rather than the URL (e.g. Hyperliquid).
}
