% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_request.R
\name{parse_json_response}
\alias{parse_json_response}
\title{Default response parser: JSON body, error on non-2xx}
\usage{
parse_json_response(resp)
}
\arguments{
\item{resp}{(class<httr2_response>) the response to parse.}
}
\value{
(any) the parsed JSON body (lists, with \code{simplifyVector = FALSE}).
}
\description{
A generic \link[httr2:response]{httr2::response} -> data parser: returns the parsed JSON body, and
raises on a non-2xx HTTP status. Connectors with a business-level error
envelope (e.g. a \code{code}/\code{msg} field that signals failure on a 200) supply their
own \code{parse_envelope} to \code{\link[=build_request]{build_request()}} instead; this is the sensible default
when the HTTP status alone tells the truth.
}
