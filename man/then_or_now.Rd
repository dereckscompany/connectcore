% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_request.R
\name{then_or_now}
\alias{then_or_now}
\title{Apply a continuation to a value or a promise}
\usage{
then_or_now(x, fn, is_async = FALSE)
}
\arguments{
\item{x}{(any) a value, or a \link[promises:promise]{promises::promise} resolving to one.}

\item{fn}{(function) applied to the (resolved) value of \code{x}.}

\item{is_async}{(scalar<logical>) if \code{TRUE}, return \code{promises::then(x, fn)};
otherwise return \code{fn(x)}. Default \code{FALSE}.}
}
\value{
(any) \code{fn(x)}, or a promise resolving to it.
}
\description{
The single sync/async branching point. Routes a value through \code{fn} either
synchronously or as a \link[promises:promise]{promises::promise}, depending on \code{is_async}. A connector
writes its methods mode-agnostically and only chooses \code{is_async} once.
}
