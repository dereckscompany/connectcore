% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{scrub_url}
\alias{scrub_url}
\title{Redact query-string credentials from a URL}
\usage{
scrub_url(
  url,
  sensitive_params = c("key", "secret", "sign", "signature", "sig", "token", "pass",
    "passphrase", "apikey", "api_key", "access", "nonce")
)
}
\arguments{
\item{url}{(scalar<character> | NULL) a URL, possibly carrying a query string.
\code{NULL} returns \code{NULL}.}

\item{sensitive_params}{(vector<character, 1..>) parameter-name tokens whose
values are redacted (matched as a case-insensitive substring of the key).}
}
\value{
(scalar<character> | NULL) the URL with sensitive query values redacted.
}
\description{
Returns \code{url} with the \emph{value} of every query parameter whose name contains a
sensitive token (case-insensitively) replaced by \verb{<redacted>}, so a URL stored
on a condition or written to a log never leaks a signature, API key, or token.
The path and the non-sensitive parameters (symbol, limit, ...) are preserved
for debugging. Matching is deliberately conservative: a substring match on the
key over-redacts a borderline name rather than risk leaking a secret.
}
\seealso{
\link{connectcore_conditions}
}
