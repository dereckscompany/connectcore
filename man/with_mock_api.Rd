% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{with_mock_api}
\alias{with_mock_api}
\title{Run code with a mock router installed}
\usage{
with_mock_api(routes, code)
}
\arguments{
\item{routes}{(list) the routes, as for \code{\link[=mock_router]{mock_router()}}.}

\item{code}{(any) the expression to evaluate with the router installed.}
}
\value{
(any) the value of \code{code}.
}
\description{
Installs \code{mock_router(routes)} as the \code{httr2_mock} option for the duration of
\code{code}, restoring the previous value afterwards (even on error). This is what a
connector's tests and vignettes call instead of hand-setting the option. Every
\code{httr2::req_perform()} / \code{req_perform_promise()} inside \code{code} is intercepted.
}
