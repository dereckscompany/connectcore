% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{mock_router}
\alias{mock_router}
\title{Build a mock HTTP router (an \code{httr2} mock hook)}
\usage{
mock_router(routes, response_builder = mock_response)
}
\arguments{
\item{routes}{(list) a list of routes. Each route is a
\verb{list(match = <scalar<character> | function>, fixture = <function | value>, method = <scalar<character> | NULL>)}. A route may name its matcher \code{pattern}
instead of \code{match} (the connectors' existing tables do). (Lists of routes,
e.g. from \code{\link[=body_routes]{body_routes()}}, can be spliced in with \code{c()}.)}

\item{response_builder}{(function) builds a response from resolved fixture data.
Default \code{\link[=mock_response]{mock_response()}}.}
}
\value{
(function) the dispatcher \verb{function(req)} for \code{options(httr2_mock = )}.
}
\description{
A factory returning a \verb{function(req)} suitable for \code{options(httr2_mock = ...)}.
The router walks \code{routes} in order and returns the first match. A route's
matcher is its \code{match} field if present, else its \code{pattern} field (the
back-compat name the existing connector tables use). The matcher is \strong{either}:
\itemize{
\item a \verb{scalar<character>} -- matched as a substring of \code{req$url}
(\code{grepl(match, req$url, fixed = TRUE)}); the URL-pattern style used by
coinbase, alpaca, binance, and kucoin; \strong{or}
\item a \verb{function(req) -> logical} -- an arbitrary predicate that may read the URL,
method, AND body, for body-routed APIs (Hyperliquid; see \code{\link[=body_routes]{body_routes()}}).
}
}
\details{
A route also carries an optional \code{method}: if set, the route matches only when
\code{req$method} equals it (so the same URL can map to different fixtures by verb).
The matched route's \code{fixture} is resolved -- called if it is a function, else
used as-is -- and passed to \code{response_builder} (an \code{httr2_response} is returned
unchanged, the pass-through path). Because a fixture is invoked per request, a
counter in its closure expresses \strong{stateful} routes (e.g. paginate: page 1
then an empty page). An unmatched request raises "Unmocked request".
}
