% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{local_mock_api}
\alias{local_mock_api}
\title{Install a mock router for the rest of the current scope}
\usage{
local_mock_api(routes, .env = parent.frame())
}
\arguments{
\item{routes}{(list) the routes, as for \code{\link[=mock_router]{mock_router()}}.}

\item{.env}{(class<environment>) the scope whose exit restores the option.
Default \code{\link[=parent.frame]{parent.frame()}}.}
}
\value{
(function) the installed dispatcher, invisibly.
}
\description{
The \code{\link[withr:with_options]{withr::local_options()}} companion to \code{\link[=with_mock_api]{with_mock_api()}}: installs
\code{mock_router(routes)} as the \code{httr2_mock} option until \code{.env} (the calling
frame by default) exits, then restores the previous value. Use it at the top of
a \code{test_that()} block (or any function) to mock every request for the remainder
of that scope without nesting the body inside a \code{code} argument.
}
