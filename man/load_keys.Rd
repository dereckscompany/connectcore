% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_env.R
\name{load_keys}
\alias{load_keys}
\title{Load API credentials from environment variables}
\usage{
load_keys(spec, warn = TRUE)
}
\arguments{
\item{spec}{(list) a named list mapping each credential field to its env var
name, e.g. \code{list(api_key = "BINANCE_API_KEY", api_secret = "BINANCE_API_SECRET")}.}

\item{warn}{(scalar<logical>) warn when a field resolves empty. Default \code{TRUE}.}
}
\value{
(list) a named list of the resolved credential values.
}
\description{
Reads a named set of credential fields from env vars into a list (the shape a
signer expects, e.g. \code{list(api_key = ..., api_secret = ...)}). Warns (rather
than aborts) if any field is empty, since public endpoints work without keys.
}
