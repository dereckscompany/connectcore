% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_ws.R
\name{ws_backoff_delay}
\alias{ws_backoff_delay}
\title{Full-jitter exponential reconnect backoff (seconds)}
\usage{
ws_backoff_delay(attempt, cap_seconds = 60)
}
\arguments{
\item{attempt}{(scalar<count in [1, Inf[>) the reconnect attempt number
(1, 2, 3, ...).}

\item{cap_seconds}{(scalar<numeric in ]0, Inf[>) maximum delay before the jitter
floor. Default \code{60}.}
}
\value{
(scalar<numeric in [1, Inf[>) a delay in seconds, always \verb{>= 1}.
}
\description{
Returns a randomised delay that grows as \code{2^attempt} but is capped, so a flood
of reconnect attempts can never trip a server's connection rate limit. The
jitter (a uniform \verb{[0, 1)} factor) de-synchronises many clients reconnecting at
once, and the \code{+1} floor guarantees a minimum spacing.
}
