% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_parse.R
\name{collapse_string_array_fields}
\alias{collapse_string_array_fields}
\title{Collapse plain-string array fields to semicolon-joined scalars}
\usage{
collapse_string_array_fields(x, fields)
}
\arguments{
\item{x}{(list) a named list (one record).}

\item{fields}{(character) names of the fields to collapse.}
}
\value{
(list) the same record with those fields collapsed in place.
}
\description{
Replaces each named field holding an array of short strings with one
\verb{;}-joined character scalar, so a record stays one row with no list columns.
\verb{;} is used (not \verb{,}) because the values are short codes/identifiers; recover
the vector with \code{strsplit(value, ";", fixed = TRUE)}. Empty/missing arrays
become \code{NA_character_}. A once-per-session warning fires if a value already
contains a \verb{;} (which a later split would corrupt).
}
