% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{abort_api_error}
\alias{abort_api_error}
\title{Raise a typed HTTP API error}
\usage{
abort_api_error(
  status,
  url = NULL,
  body = NULL,
  message = NULL,
  max_body = 2048L
)
}
\arguments{
\item{status}{(scalar<count in [100, 599]>) the HTTP status code. Also names
the most specific class, \verb{connectcore_api_error_<status>}.}

\item{url}{(scalar<character> | NULL) the request URL; query-string credentials
are redacted with \code{\link[=scrub_url]{scrub_url()}} before storing on the \code{url} field. Default
\code{NULL}.}

\item{body}{(scalar<character> | NULL) the response body text; stored truncated
to \code{max_body} characters on the \code{body_snippet} field (named \code{body_snippet},
not \code{body}, because \code{rlang::abort()} reserves \code{body}). Default \code{NULL}.}

\item{message}{(scalar<character> | NULL) the condition message. \code{NULL}
(default) derives the byte-identical legacy string from \code{status} and \code{body}.}

\item{max_body}{(scalar<count in [1, Inf[>) truncate the stored body snippet
to this many characters. Default \code{2048}.}
}
\value{
(class<connectcore_error>) never returns normally; signals the classed
condition described above.
}
\description{
Signals a condition classed \verb{c("connectcore_api_error_<status>",}
\verb{"connectcore_api_error", "connectcore_error")} (on top of rlang's error
classes), carrying the HTTP \code{status}, the request \code{url} (query-string
credentials redacted), and a truncated \code{body_snippet} as structured fields. The
message defaults to the byte-identical \code{"HTTP error <status>\\n<body>"} the
transport base signalled before typed conditions existed, so nothing that
matched on message text breaks. See \link{connectcore_conditions} for the taxonomy
and the connector-subclass recipe.
}
\seealso{
\link{connectcore_conditions}
}
