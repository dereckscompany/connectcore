% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{body_routes}
\alias{body_routes}
\title{Build body-discriminated routes from a named case table}
\usage{
body_routes(url_filter, field_path, cases)
}
\arguments{
\item{url_filter}{(scalar<character>) a substring the request URL must contain.}

\item{field_path}{(character) the path of names to the discriminating body
field (e.g. \code{c("action", "type")} for \code{body$action$type}).}

\item{cases}{(list) a \strong{named} list of fixtures keyed by the body field's
value; each value is a fixture (a function, a built \code{httr2_response}, or data).}
}
\value{
(list) a list of routes, one per \code{names(cases)}, each a
\verb{list(match = <predicate>, fixture = <value>, method = NULL)}.
}
\description{
The ergonomic constructor for body-routed APIs (e.g. Hyperliquid, whose whole
REST surface sits behind two POST paths, each dispatched by a field in the JSON
body). Given a URL filter, a body field path, and a \strong{named} list of fixtures
keyed by that field's value, it returns one predicate-route per case: each
matches when \code{req$url} contains \code{url_filter} AND the body field at \code{field_path}
equals that case's name.
}
\details{
For example, Hyperliquid's \verb{/info} reads (keyed by \code{body$type}) and \verb{/exchange}
writes (keyed by \code{body$action$type}) become:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mock_router(c(
  body_routes("/exchange", c("action", "type"), exchange_routes),
  body_routes("/info", "type", info_routes)
))
}\if{html}{\out{</div>}}
}
