% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{abort_response_error}
\alias{abort_response_error}
\title{Raise a typed response-parse error}
\usage{
abort_response_error(
  message,
  field = NULL,
  url = NULL,
  body = NULL,
  max_body = 2048L
)
}
\arguments{
\item{message}{(scalar<character>) the condition message (passed through
verbatim).}

\item{field}{(scalar<character> | NULL) the missing / malformed field name, if
known. Default \code{NULL}.}

\item{url}{(scalar<character> | NULL) the request URL; query-string credentials
are redacted with \code{\link[=scrub_url]{scrub_url()}} before storing on the \code{url} field. Default
\code{NULL}.}

\item{body}{(scalar<character> | NULL) the response body text; stored truncated
to \code{max_body} characters on the \code{body_snippet} field (named \code{body_snippet},
not \code{body}, because \code{rlang::abort()} reserves \code{body}). Default \code{NULL}.}

\item{max_body}{(scalar<count in [1, Inf[>) truncate the stored body snippet
to this many characters. Default \code{2048}.}
}
\value{
(class<connectcore_error>) never returns normally; signals the classed
condition described above.
}
\description{
Signals a condition classed \verb{c("connectcore_response_error",}
\verb{"connectcore_error")} for a response that reached the client but is malformed
or missing an expected field (e.g. a server-time endpoint whose payload lacks
its time key). Carries the offending \code{field}, the request \code{url} (credentials
redacted), and a truncated \code{body} snippet. The \code{message} is passed through
verbatim, so an existing string stays byte-identical. See
\link{connectcore_conditions} for the taxonomy.
}
\seealso{
\link{connectcore_conditions}
}
