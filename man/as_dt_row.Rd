% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_parse.R
\name{as_dt_row}
\alias{as_dt_row}
\title{Convert a named list to a one-row data.table}
\usage{
as_dt_row(x)
}
\arguments{
\item{x}{(list | NULL) a named list (one record).}
}
\value{
(class<data.table>) a one-row data.table (empty if \code{x} is \code{NULL}/empty).
}
\description{
Turns a flat named list (one JSON object) into a single-row
\link[data.table:data.table]{data.table::data.table}: \code{NULL} becomes \code{NA}, names are snake_cased, and any
nested value is wrapped as a single list-column cell so the row never widens
unexpectedly (per-endpoint parsers flatten nesting themselves).
}
