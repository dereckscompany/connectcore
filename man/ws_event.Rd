% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_ws.R
\name{ws_event}
\alias{ws_event}
\title{Construct a typed WebSocket lifecycle event}
\usage{
ws_event(event_type, fields = list())
}
\arguments{
\item{event_type}{(scalar<character in c("open", "message", "error", "close", "reconnect")>)
the event type; also the \code{event_type} field.}

\item{fields}{(list) per-type fields merged into the event after \code{event_type}
and \code{timestamp}. Default \code{list()}.}
}
\value{
(list) the lifecycle event: \code{event_type}, \code{timestamp} (POSIXct/UTC),
then the \code{fields}.
}
\description{
Builds the structured event object \link{StreamClient} delivers to an \verb{$on_event()}
handler: a \code{list} with an \code{event_type} (one of \link{WS_EVENT_TYPES}), a lubridate
UTC \code{timestamp}, and the per-type fields merged in. It is the WebSocket analogue
of the typed REST conditions (see \code{?connectcore_conditions}): a caller branches
on \code{event$event_type} and reads fields as data instead of re-parsing a raw
callback payload. A WebSocket connector can also build one directly — e.g. to
emit a parsed error frame as a structured \code{"error"} event.
}
\details{
Per-type fields the base populates: \code{open} carries \code{reconnect} (was this a
reconnect); \code{message} carries \code{data} (the frame text); \code{close} carries \code{code}
and \code{reason}; \code{error} carries \code{error} (the socket error event); \code{reconnect}
carries \code{attempt} and \code{delay} (the backoff seconds).
}
