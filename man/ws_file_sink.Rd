% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_ws.R
\name{ws_file_sink}
\alias{ws_file_sink}
\title{A message handler that appends each frame to a connection}
\usage{
ws_file_sink(con)
}
\arguments{
\item{con}{(class<connection>) an open, writable connection (e.g. from
\code{\link[base:connections]{base::file()}} in append mode).}
}
\value{
(function) a handler \verb{function(message)} suitable for
\code{StreamClient$on("message", ...)}.
}
\description{
Convenience for the recorder hot path: returns a \code{"message"} handler that
writes each frame followed by a newline to an open connection, doing the
absolute minimum so the socket is drained fast (a slow handler can get a
connection dropped by the server). Hourly rotation / flushing is the caller's
job — pass a connection you \code{flush()} and rotate yourself.
}
